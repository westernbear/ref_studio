# Learnings — reference-video-studio-saas

Conventions, patterns, and successful approaches discovered during work on this plan.

_Auto-scaffolded by /start-work. Append new entries below - never overwrite._

---

- Task 5 uses an explicit `schema_migrations` table and SQL migration; startup never auto-syncs schema.
- Tenant fencing is encoded with composite `(tenant_id, id)` foreign keys in addition to ordinary foreign keys, so a resource ID cannot cross tenant boundaries.
- SQLite single-host policy is centralized in `database/db.mjs`: WAL, foreign keys, 5-second busy timeout, absolute local path assertion, and migration transactions use `BEGIN IMMEDIATE`.
- Append-only receipt, audit, and IR history is enforced by SQLite triggers; deletion epochs are monotonic by trigger.
- Task 2 vendors the pinned Inter and Wanted Sans artifacts from `runtime/hydrated`; Manrope and Geist faces are stored under `apps/web/public/fonts` and all runtime font URLs are local.
- `pnpm assets:verify` validates manifest metadata, local paths, and SHA-256 digests; the missing-font fixture intentionally resolves Wanted Sans to a non-existent fixture path and emits `RUNTIME_PREREQUISITE_MISSING`.
- Task 3 freezes 151 unique controls and binds the materialized contract lock plus verifier root to the current parent authority-root SHA; the self-test exercises all required negative-case categories without touching product code.
- Task 4 reuses the existing FFmpeg lock compiler in non-writing mode: two passes reproduce pass digest `ea2a41df...45d9b4`, while the fixture lock remains byte-identical.
- Task 4 generates all 16 contract IDs and writes an independent `variants/manifest.json` for the ablation fixture; ffprobe confirms the dense OCR stop fixture is 3840x2160 at 60fps with 240 frames.
- Task 6 centralizes all branded IDs and literal enums in `packages/contracts/src`, with lifecycle transitions kept as one table so API and worker callers share the same legality rules.
- Task 6 uses Zod parsing for public projections and IR owner checks; internal states are never accepted by creator projection schemas, and safe error normalization discards unknown error details.
- Task 17 compiler preflight validates every frozen supply artifact hash, CPU-only Torch pins, local fonts/models, disk admission, and the 12 GiB RSS contract without network access.
- Task 17 capacity probe verifies all admitted 96/100/120/200/240-frame fixtures with the primary 540x960 profile and native OCR-crop requirement; the 240-frame fixture is explicitly checked as 3840x2160 dense OCR.
- Task 18 extracts every frame from a normalized-only artifact through an injected deterministic provider, preserves Fraction FPS and native references, and rejects sequence, interval, resource, cancellation, and provenance violations fail-closed.
- Task 26 adds a strict render-app WebGL2 adapter boundary with immutable shader contracts, explicit context/limit probes, compile/link diagnostics, deterministic pass consumption, independent bloom/defocus inputs, and separate residual-canvas ownership/order validation.
- Task 7 auth uses scrypt password hashes and SHA-256 bearer lookup, with principals derived from stored users, memberships, and release assignments; browser sessions use an opaque HttpOnly/Secure/Lax cookie and 30-minute idle expiry.
- Task 7 BFF introspection is service-credential-only and returns only opaque session material plus the CSRF assertion; route tests prove roles, capabilities, and tenant principal claims are absent, while rotation/logout invalidate prior cookies.
- Task 8 centralizes request/resource fencing, deletion-epoch checks, correlation-safe envelopes, and scoped idempotency replay in `apps/api/src/boundary.ts`; the direct `/v1` hook excludes only the release-review route, which uses release-scope auth without tenant membership comparison.
- Task 9 keeps upload bytes in bounded per-chunk quarantine buffers, incrementally hashes accepted chunks, and keys CAS identity by `tenantId:sha256`; finalization only transitions to `ACCEPTED` after the MP4 `ftyp` precheck.
- Task 10 adds an injectable media sandbox boundary: ffprobe/ffmpeg are represented only as argument arrays with explicit timeout/output caps and a no-network, read-only-root, unprivileged policy. Tests use deterministic synthetic probe/normalization results because the API process must not execute media tools.
- Task 10 validates accepted T9 records only, preserves source CAS identity, and creates a separate normalized digest/provenance projection. Exact 4-second intervals use half-open source-frame bounds (`[startFrame, startFrame + fps*4)`) and absent audio becomes explicit 48kHz stereo synthesized silence metadata.
- Task 11 adds an injectable creator workflow store/router and keeps `/v1/reviews` unregistered; creator projections omit creator identity and artifact reads fail closed until publication.
- Creator job intake requires explicit frame metadata so the exact four-second half-open interval can be checked at the API boundary rather than accepted through an overflow-only predicate.
- Task 12 admin reads use an optional injectable read store and derive ops/viewer tenant scope only from server-side tenant assignments; super-admin receives platform scope.
- Task 12 projections omit job private paths, quarantine raw bytes/private paths, and billing payment data is always replaced by a REDACTED marker; sensitive successful reads append auth-store audit events.
- Task 12 required hardening after initial smoke verification: assigned viewer, unassigned operator, creator-only denial, all six route reads, exact sensitive-read audits, forbidden field-query denial, and a bounded in-memory query counter are now covered.
- Task 13 adds a separate injectable admin mutation store and allowlisted routes; every successful mutation is executed through scoped idempotency and appends one audit event carrying before/after state, reason, correlation, and outcome. Queue controls remain super-admin-only and Prioritize is denied as an authoritative-queue operation.
- Task 14 adds an explicit injectable review store so designated reviewer decisions are append-only, sequence-ordered, predecessor-linked T1-T6 receipts; `/v1/reviews` handles tenant T1-T5 and `/v1/release-reviews` handles release T6 without widening the legacy auth boundary when the store is absent.
- Task 15 adds a worker-owned SQLite queue schema with a singleton drain control, deterministic `(eligible_at, created_at, job_id)` ordering, `BEGIN IMMEDIATE` claims, DB-time lease expiry, random token fencing, and append-only attempt rows.
- 2026-08-22 Task 25: render-app uses a pure semantic SVG serializer keyed only by frame, SceneIR geometry/lifecycle, BrowserPassSpec layer order, and an explicit seed; capture delegates to the same renderFrame source and read-back metadata is checked exactly.
- 2026-08-22 Task 30: the 151-row UI projection is generated from the frozen JSONL oracle; only selectors and derived API operation IDs are added, with concrete fixture IDs canonicalized to contract path parameters. Shared primitives expose semantic disabled state, disabled reasons, keyboard-focus styling, and creator/admin navigation shells.
 - 2026-08-22 Task 30 build fix: `ActionButton` now constructs `ControlProps` with `disabledReason` only when defined, satisfying `exactOptionalPropertyTypes` while retaining conditional `aria-describedby`; focused component tests and Next production build pass.
- 2026-08-22 Task 31: the REF_STUDIO landing uses the existing token system and Task 30 control metadata; public destinations are bounded through a typed catch-all route, while New Project checks the opaque session cookie and redirects unauthenticated users with a return URL.
- 2026-08-22 Task 31 verification: landing coverage belongs under `apps/web/test/landing.e2e.spec.ts` because Playwright discovery is restricted to `**/*.e2e.spec.ts`; the suite covers controls, drawers, bounded routes, health, and session-sensitive New Project routing.
- 2026-08-22 Task 31 browser verification: Playwright now launches `/usr/bin/google-chrome` on isolated port 3100, avoiding the stale port-3000 dev server; all 3 `@landing` tests pass without downloading browsers.
- 2026-08-22 Task 27: capture boundary pins Chromium 151.0.7922.138, SwiftShader/OpenGL, display/locale/timezone/seed/origin settings, and rejects failed runtime/font/model/WebGL/shader/network probes before invoking capture; repeated PNG bytes and SHA-256 are recorded.
- 2026-08-22 Task 33: `/projects/new` now uses a real labeled MP4 input with click, keyboard, and drag/drop paths; upload-client sequences bounded quarantine upload, chunking, finalization, and idempotent `/v1/jobs` admission only after accepted normalized media and a valid half-open four-second interval.
- 2026-08-22 Task 28: delivery assembly accepts exactly contiguous frames 0-119 plus 192000 stereo samples/channel and stages output privately as AWAITING_T5 after fsync, digest, rename, parent fsync, and DB insertion.
- 2026-08-22 Task 29: job-attempt lifecycle is separate from upload sessions; accepted upload admission starts PREPARING, and orchestration keeps gate approval progress independent from render frame progress through private AWAITING_T5 staging.
- 2026-08-22 Task 35: `/admin/jobs` keeps queue filters/search/pagination in client state across refresh and detail interactions; cancellation transitions visibly to `CANCEL_REQUESTED`, while attempts and receipts remain append-only in the detail projection.
- 2026-08-22 Task 35: delivery buttons are enabled only for `COMPLETED` plus `PUBLISHED`; staged, failed, partial, foreign, and expired artifacts are represented as unavailable without exposing private paths.
- 2026-08-22 Task 34: scene review keeps OBSERVED measurements display-only; topology/owner edits set a stale-review state, preserve receipt history, and require the designated reviewer to restart the current predecessor gate rather than batch-approving T1-T3.
- 2026-08-22 Task 36: the shared admin shell now owns platform scope context, bounded operations links, desktop sidebar, and a labeled narrow-screen menu; `/admin` uses deterministic scoped operational data while `/admin/jobs` preserves Task 35's 30-control queue contract.
- 2026-08-22 Task 37: tenant detail keeps canonical IDs in internal links and models role/suspension mutation state separately from the deterministic read projection; drawer activity rows link to bounded tenant-filtered audit views without exposing private media or paths.
- 2026-08-22 Task 37 Atlas repair: source controls 7, 8, 12, and 17 are now meaningful first-row/card/activity controls, and the E2E collects IDs across conditional surfaces before asserting the exact unique 1..23 contract.
- 2026-08-22 Task 38: ReceiptChain keeps immutable receipt projections deterministic, uses query-string selection with list fallback and keyboard traversal, and exports only safe scoped metadata through a short-lived browser download.
- 2026-08-22 Task 39: QuarantineReview keeps canonical qitem_* IDs in URL/action state while rendering QT-* only as display IDs; evidence is bounded to safe metadata and release/reject confirmations preserve explicit isolation semantics.
- 2026-08-22 Task 39 Atlas correction: controls 9-13 now inspect role/scope, tenant fencing, isolation policy, audit policy, and copy canonical IDs; controls 14-15 are confirmation dialog actions, and E2E asserts per-surface uniqueness.
- 2026-08-22 Task 40: AuditLog keeps deterministic newest-first safe projections, preserves filters across detail/export, and maps the frozen admin_audit:1..15 contract without mutation affordances.
- 2026-08-22 Task 40 Atlas repair: all 15 controls now represent audit behavior rather than generic navigation; E2E collects unique visible IDs across filters, drawer, export, pagination, and viewer scope.
- 2026-08-22 Task 41: frozen 151-control coverage now passes at 1440x900 and 390x844. Dashboard-only scope details and repeated quarantine row actions were correctly excluded from the oracle; audit export/detail and tenant drawer/access states are collected through their real conditional flows.
- 2026-08-22 Task 41 diagnostics: the web test project now includes a local `node-shims.d.ts` declaration for the existing Node fs/path/process APIs, avoiding an offline dependency install while keeping the harness runtime unchanged.
- 2026-08-22 Task 42: added Playwright-backed `test:a11y` and `test:visual` runners. Accessibility passed 11/11 route surfaces with axe WCAG 2 A/AA, keyboard focus, reduced-motion, and overflow checks; visual structural checks passed all nine frozen screen mappings at 1440x900, 390x844, 320, and 768 widths.
- 2026-08-22 Task 44: bounded local pilot scripts admit 24/25/30/50/60fps as exact four-second half-open intervals with 96/100/120/200/240 frames; the 60fps 3840x2160 dense-OCR fixture is the explicit 240-frame capacity boundary. Two deterministic integrated repeats cover upload, compiler, T1-T5, renderer, media QC, publication, and authorized download semantics with fixed CPU/RSS/timing baselines.
- 2026-08-22 Task 45: deployment verification now checks the frozen execution contract, Compose isolation, explicit migration/preflight order, and generated OpenAPI before any pilot traffic.
- 2026-08-22 Task 45: recovery verification uses a new temporary root, increments restoreEpoch, revokes credentials/leases/downloads, preserves deletion epochs, verifies sequence/CAS/fixed frames, and gates release-scoped T6 on an immutable baseline.
- 2026-08-22 Task 45: handoff packaging is explicit-allowlist only; the ZIP contains contracts, manifests, evidence summaries, docs, and verification scripts, never secrets, raw media, runtime DBs, caches, model weights, or private paths.
- 2026-08-22 Task 45 follow-up: normalized handoff staging mtimes, used `zip -X -D`, and confirmed two consecutive builds produce the same SHA-256 (`48890e333d672c3d62ca051f218e3ce659f208af904be53aa94b329b035557a0`), synchronized into Task 45 evidence.
- 2026-08-22 final-wave infrastructure: `verification/blackbox/final.mjs` now implements explicit f1/f2/f3/f4 modes; F2-F4 append attempt-local receipts and F1 requires `--canonical-write` before touching the canonical index. F2/F3/F4 local modes pass with 151 controls, five OpenAPI paths, five frozen model entries, high=0/critical=0, and truthful Task 42 structural/WCAG fallback.
- 2026-08-22 final-wave infrastructure: recreated pnpm virtual-store links offline from the existing local store after a root-owned `.pnpm` tree blocked hydration; no package download occurred. Typecheck, API/worker security suites, and compiler pytest then passed.

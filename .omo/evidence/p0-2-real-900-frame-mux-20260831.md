# P0.2 real 900-frame H.264/AAC mux — 2026-08-31

- tip (root at run): `c206711`
- worker tree: `/home/singlerr/ref_studio-motion-complete/apps/worker`
- command: `RVS_REAL_900_MUX=1 pnpm exec vitest run src/generated-video-delivery.real.test.ts`
- result: **PASS** in 22.3s
- test: `apps/worker/src/generated-video-delivery.real.test.ts`

## What ran

`assembleGeneratedVideo()` with the production `runCommand` runner (not a mocked `CommandRunner`):

- 900 hardlinked 1080×1920 seed PNGs
- libx264 High 4.1, yuv420p, GOP 60, bt709
- AAC LC stereo 48 kHz 192k (`anullsrc` deterministic silence)
- separate ffprobe documents: metadata `v:0`, frames `v:0`, audio `a:0`

Recorded PASS metadata (asserted): `status=PASS`, `width=1080`, `height=1920`, `fps=30`, `frameCount=900`, `videoCodec=h264`, `audioCodec=aac`.

The default unit suite still keeps the mocked 64 KiB stdout regression. This opt-in test is the real mux gate (`RVS_REAL_900_MUX=1`).
